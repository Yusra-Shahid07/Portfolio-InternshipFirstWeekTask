{
  "name": "portfolio-internship-first-week-task",
  "private": true,
  "scripts": {
    "build": "npm --prefix myportfolio install && npm --prefix myportfolio run build",
    "vercel-build": "npm --prefix myportfolio install && npm --prefix myportfolio run build"
  }
}
